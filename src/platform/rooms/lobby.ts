import {ensureAnonymousSession} from '../auth/session';
import {getSupabaseClient} from '../supabase/client';
export interface LobbyPlayer{userId:string;displayName:string;playerColor:string|null;turnOrder:number|null;isReady:boolean;isHost:boolean}
export interface Lobby{room:{id:string;code:string;status:string;gameId:string;isCurrentUserHost:boolean};players:LobbyPlayer[]}
export class LobbyReadError extends Error{constructor(message="You don't have access to this room."){super(message);this.name='LobbyReadError'}}
export async function getLobby(roomCode:string,client=getSupabaseClient()):Promise<Lobby>{const {user}=await ensureAnonymousSession(client);const code=roomCode.trim().toUpperCase();if(!/^[A-Z0-9]{6,10}$/.test(code))throw new LobbyReadError('That room code is not valid.');const roomResult=await client.from('rooms').select('id,code,status,game_id,host_user_id').eq('code',code).maybeSingle();if(roomResult.error||!roomResult.data)throw new LobbyReadError();const room=roomResult.data as Record<string,unknown>;if(typeof room.id!=='string'||typeof room.code!=='string'||typeof room.status!=='string'||typeof room.game_id!=='string'||typeof room.host_user_id!=='string')throw new LobbyReadError();const playerResult=await client.from('room_players').select('user_id,display_name,player_color,turn_order,is_ready').eq('room_id',room.id).order('joined_at');if(playerResult.error||!Array.isArray(playerResult.data))throw new LobbyReadError();const players=playerResult.data.map(value=>{const player=value as Record<string,unknown>;if(typeof player.user_id!=='string'||typeof player.display_name!=='string'||typeof player.is_ready!=='boolean')throw new LobbyReadError();return{userId:player.user_id,displayName:player.display_name,playerColor:typeof player.player_color==='string'?player.player_color:null,turnOrder:typeof player.turn_order==='number'?player.turn_order:null,isReady:player.is_ready,isHost:player.user_id===room.host_user_id};});return{room:{id:room.id,code:room.code,status:room.status,gameId:room.game_id,isCurrentUserHost:room.host_user_id===user.id},players};}
